<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    android:orientation="vertical"
    android:id="@+id/cloud_web_view_layout"
    android:fitsSystemWindows="true"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <WebView
        android:id="@+id/cloud_view"
        android:visibility="gone"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_above="@+id/panel_back" />
    <ProgressBar
        android:id="@+id/progressBar"
        android:padding="2.0dip"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="120.0dip"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:indeterminateTint="#ff444444"
        style="?android:progressBarStyle" />
    <LinearLayout
        android:orientation="horizontal"
        android:id="@+id/panel_back"
        android:background="@color/link_color"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:showDividers="middle"
        style="?android:buttonBarStyle">
        <Button
            android:id="@+id/back_button"
            android:text="@string/label_back"
            style="@style/Web_White_ButtonStyle" />
    </LinearLayout>
</RelativeLayout>